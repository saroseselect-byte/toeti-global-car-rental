const json=(data,status=200,extra={})=>new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store',...extra}});
const now=()=>new Date().toISOString();
const enc=new TextEncoder();

function requiredEnv(env,names){for(const n of names)if(!env[n])return n;return null}
function adminOk(req,env){const token=req.headers.get('x-toeti-admin-token')||'';return !!env.TOETI_ADMIN_TOKEN&&token===env.TOETI_ADMIN_TOKEN}
function formBody(obj){const p=new URLSearchParams();for(const [k,v] of Object.entries(obj)){if(v===undefined||v===null)continue;p.append(k,String(v))}return p}
async function stripeRequest(env,path,method='POST',body){const missing=requiredEnv(env,['STRIPE_SECRET_KEY']);if(missing)throw new Error(`Missing ${missing}`);const r=await fetch(`https://api.stripe.com/v1${path}`,{method,headers:{authorization:`Bearer ${env.STRIPE_SECRET_KEY}`,'content-type':'application/x-www-form-urlencoded'},body:method==='GET'?undefined:formBody(body)});const data=await r.json();if(!r.ok)throw new Error(data?.error?.message||`Stripe ${r.status}`);return data}
async function verifyStripeSignature(raw,header,secret){if(!header||!secret)return false;const parts=Object.fromEntries(header.split(',').map(x=>x.split('=').map(y=>y.trim())));const t=parts.t,v1=parts.v1;if(!t||!v1)return false;const age=Math.abs(Date.now()/1000-Number(t));if(!Number.isFinite(age)||age>300)return false;const key=await crypto.subtle.importKey('raw',enc.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);const sig=await crypto.subtle.sign('HMAC',key,enc.encode(`${t}.${raw}`));const hex=[...new Uint8Array(sig)].map(b=>b.toString(16).padStart(2,'0')).join('');if(hex.length!==v1.length)return false;let diff=0;for(let i=0;i<hex.length;i++)diff|=hex.charCodeAt(i)^v1.charCodeAt(i);return diff===0}
async function getOrder(env,bookingId){return env.DB.prepare('SELECT * FROM payment_orders WHERE booking_id=?').bind(bookingId).first()}
async function writeOrder(env,o){const ts=now();await env.DB.prepare(`INSERT INTO payment_orders (id,booking_id,vertical,supplier_slug,supplier_name,title,amount_minor,currency,customer_email,supplier_connect_account,supplier_share_bps,status,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(booking_id) DO UPDATE SET vertical=excluded.vertical,supplier_slug=excluded.supplier_slug,supplier_name=excluded.supplier_name,title=excluded.title,amount_minor=excluded.amount_minor,currency=excluded.currency,customer_email=excluded.customer_email,supplier_connect_account=excluded.supplier_connect_account,supplier_share_bps=excluded.supplier_share_bps,status='supplier_confirmed',updated_at=excluded.updated_at`).bind(crypto.randomUUID(),o.booking_id,o.vertical,o.supplier_slug,o.supplier_name,o.title,o.amount_minor,String(o.currency).toLowerCase(),o.customer_email||null,o.supplier_connect_account||null,o.supplier_share_bps||8500,'supplier_confirmed',ts,ts).run();return getOrder(env,o.booking_id)}
async function confirmOrder(req,env){if(!adminOk(req,env))return json({error:'unauthorized'},401);const b=await req.json();for(const k of ['booking_id','vertical','supplier_slug','supplier_name','title','amount_minor','currency'])if(!b[k])return json({error:`missing_${k}`},400);if(!['cars','experiences'].includes(b.vertical))return json({error:'invalid_vertical'},400);if(!Number.isInteger(Number(b.amount_minor))||Number(b.amount_minor)<=0)return json({error:'invalid_amount'},400);const share=Number(b.supplier_share_bps||8500);if(share<0||share>10000)return json({error:'invalid_supplier_share_bps'},400);const order=await writeOrder(env,{...b,amount_minor:Number(b.amount_minor),supplier_share_bps:share});return json({ok:true,order})}
async function createCheckout(req,env){const b=await req.json();if(!b.booking_id)return json({error:'missing_booking_id'},400);const o=await getOrder(env,b.booking_id);if(!o)return json({error:'booking_not_confirmed'},409);if(!['supplier_confirmed','checkout_created','payment_failed'].includes(o.status))return json({error:'booking_not_payable',status:o.status},409);if(o.stripe_checkout_session_id&&o.status==='checkout_created'){const s=await stripeRequest(env,`/checkout/sessions/${o.stripe_checkout_session_id}`,'GET');return json({ok:true,reused:true,checkout_url:s.url,session_id:s.id})}
const origin=new URL(req.url).origin;const meta={booking_id:o.booking_id,supplier_slug:o.supplier_slug,vertical:o.vertical};const body={'mode':'payment','success_url':`${origin}/payment/success?session_id={CHECKOUT_SESSION_ID}`,'cancel_url':`${origin}/payment/cancel?booking_id=${encodeURIComponent(o.booking_id)}`,'line_items[0][price_data][currency]':o.currency,'line_items[0][price_data][unit_amount]':o.amount_minor,'line_items[0][price_data][product_data][name]':o.title,'line_items[0][quantity]':1,'payment_intent_data[transfer_group]':`toeti_${o.booking_id}`,'metadata[booking_id]':meta.booking_id,'metadata[supplier_slug]':meta.supplier_slug,'metadata[vertical]':meta.vertical,'payment_intent_data[metadata][booking_id]':meta.booking_id,'payment_intent_data[metadata][supplier_slug]':meta.supplier_slug,'payment_intent_data[metadata][vertical]':meta.vertical};if(o.customer_email)body.customer_email=o.customer_email;const s=await stripeRequest(env,'/checkout/sessions','POST',body);await env.DB.prepare('UPDATE payment_orders SET stripe_checkout_session_id=?, status=?, updated_at=? WHERE booking_id=?').bind(s.id,'checkout_created',now(),o.booking_id).run();return json({ok:true,checkout_url:s.url,session_id:s.id})}
async function webhook(req,env){const raw=await req.text();const ok=await verifyStripeSignature(raw,req.headers.get('stripe-signature'),env.STRIPE_WEBHOOK_SECRET);if(!ok)return json({error:'invalid_signature'},400);const e=JSON.parse(raw),obj=e.data?.object||{};if(e.type==='checkout.session.completed'||e.type==='checkout.session.async_payment_succeeded'){const bid=obj.metadata?.booking_id; if(bid)await env.DB.prepare('UPDATE payment_orders SET stripe_checkout_session_id=?, stripe_payment_intent_id=?, status=?, updated_at=? WHERE booking_id=?').bind(obj.id,obj.payment_intent||null,'paid',now(),bid).run()}else if(e.type==='checkout.session.async_payment_failed'){const bid=obj.metadata?.booking_id;if(bid)await env.DB.prepare('UPDATE payment_orders SET status=?, updated_at=? WHERE booking_id=?').bind('payment_failed',now(),bid).run()}else if(e.type==='payment_intent.payment_failed'){const bid=obj.metadata?.booking_id;if(bid)await env.DB.prepare('UPDATE payment_orders SET stripe_payment_intent_id=?, status=?, updated_at=? WHERE booking_id=?').bind(obj.id,'payment_failed',now(),bid).run()}else if(e.type==='charge.refunded'){const bid=obj.metadata?.booking_id;if(bid)await env.DB.prepare('UPDATE payment_orders SET stripe_charge_id=?, status=?, updated_at=? WHERE booking_id=?').bind(obj.id,'refunded',now(),bid).run()}return json({received:true})}
async function releaseSupplier(req,env){if(!adminOk(req,env))return json({error:'unauthorized'},401);const b=await req.json();if(!b.booking_id)return json({error:'missing_booking_id'},400);const o=await getOrder(env,b.booking_id);if(!o)return json({error:'unknown_booking'},404);if(o.status!=='paid')return json({error:'not_ready_for_transfer',status:o.status},409);if(!o.supplier_connect_account)return json({error:'supplier_connect_account_missing'},409);if(o.stripe_transfer_id)return json({ok:true,reused:true,transfer_id:o.stripe_transfer_id});const pi=await stripeRequest(env,`/payment_intents/${o.stripe_payment_intent_id}`,'GET');const charge=pi.latest_charge;if(!charge)return json({error:'charge_not_available'},409);const amount=Math.floor(o.amount_minor*o.supplier_share_bps/10000);const t=await stripeRequest(env,'/transfers','POST',{amount,currency:o.currency,destination:o.supplier_connect_account,source_transaction:charge,transfer_group:`toeti_${o.booking_id}`,'metadata[booking_id]':o.booking_id,'metadata[supplier_slug]':o.supplier_slug});await env.DB.prepare('UPDATE payment_orders SET stripe_charge_id=?, stripe_transfer_id=?, status=?, updated_at=? WHERE booking_id=?').bind(charge,t.id,'supplier_paid',now(),o.booking_id).run();return json({ok:true,transfer_id:t.id,amount_minor:amount,currency:o.currency})}
async function refundOrder(req,env){if(!adminOk(req,env))return json({error:'unauthorized'},401);const b=await req.json();if(!b.booking_id)return json({error:'missing_booking_id'},400);const o=await getOrder(env,b.booking_id);if(!o)return json({error:'unknown_booking'},404);if(!o.stripe_payment_intent_id)return json({error:'payment_not_found'},409);if(o.stripe_transfer_id){const reversalAmount=Math.floor(o.amount_minor*o.supplier_share_bps/10000);await stripeRequest(env,`/transfers/${o.stripe_transfer_id}/reversals`,'POST',{amount:reversalAmount,'metadata[booking_id]':o.booking_id})}const r=await stripeRequest(env,'/refunds','POST',{payment_intent:o.stripe_payment_intent_id,'metadata[booking_id]':o.booking_id});await env.DB.prepare('UPDATE payment_orders SET stripe_refund_id=?, status=?, updated_at=? WHERE booking_id=?').bind(r.id,'refunded',now(),o.booking_id).run();return json({ok:true,refund_id:r.id,status:r.status})}
async function status(req,env){const u=new URL(req.url),bid=u.searchParams.get('booking_id');if(!bid)return json({error:'missing_booking_id'},400);const o=await getOrder(env,bid);if(!o)return json({error:'unknown_booking'},404);return json({booking_id:o.booking_id,status:o.status,currency:o.currency,amount_minor:o.amount_minor,supplier_slug:o.supplier_slug})}
function success(){return new Response('<!doctype html><html><meta name="viewport" content="width=device-width"><title>Payment received · TOETI</title><body style="font-family:system-ui;background:#f7f4ed;color:#17242b"><main style="max-width:720px;margin:14vh auto;padding:28px;background:#fff;border-radius:22px"><h1>Payment received</h1><p>Thank you. TOETI is confirming your booking details.</p><a href="/">Back to TOETI</a></main></body></html>',{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}})}
function cancel(){return new Response('<!doctype html><html><meta name="viewport" content="width=device-width"><title>Payment not completed · TOETI</title><body style="font-family:system-ui;background:#f7f4ed;color:#17242b"><main style="max-width:720px;margin:14vh auto;padding:28px;background:#fff;border-radius:22px"><h1>Payment not completed</h1><p>No payment was confirmed. You can return to TOETI and try again.</p><a href="/">Back to TOETI</a></main></body></html>',{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}})}

export default {async fetch(req,env){if(!env.DB)return json({error:'database_unavailable'},503);const u=new URL(req.url);try{if(u.pathname==='/api/payments/health')return json({ok:true,mode:'sandbox-ready-code',stripe_secret:!!env.STRIPE_SECRET_KEY,webhook_secret:!!env.STRIPE_WEBHOOK_SECRET,admin_token:!!env.TOETI_ADMIN_TOKEN});if(u.pathname==='/api/payments/orders/confirm'&&req.method==='POST')return confirmOrder(req,env);if(u.pathname==='/api/payments/checkout'&&req.method==='POST')return createCheckout(req,env);if(u.pathname==='/api/payments/webhook'&&req.method==='POST')return webhook(req,env);if(u.pathname==='/api/payments/release'&&req.method==='POST')return releaseSupplier(req,env);if(u.pathname==='/api/payments/refund'&&req.method==='POST')return refundOrder(req,env);if(u.pathname==='/api/payments/status'&&req.method==='GET')return status(req,env);if(u.pathname==='/payment/success'&&req.method==='GET')return success();if(u.pathname==='/payment/cancel'&&req.method==='GET')return cancel();return json({error:'not_found'},404)}catch(e){return json({error:'payment_engine_error',message:e.message},500)}};
