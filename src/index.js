import { vehicles, supplier, policies } from './data.js';
import { quoteCents, publicReference, secureToken, PaymentRouter } from './domain.js';

const json = (data, status = 200) => new Response(JSON.stringify(data), { status, headers: { 'content-type': 'application/json; charset=utf-8', 'cache-control': 'no-store' } });
const escapeHtml = (s='') => String(s).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));

function page() {
  const cards = vehicles.map(v => `<article class="car"><div class="badge">Request to book</div><div class="carart">${v.name.includes('RAV4')?'R4':v.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><h3>${escapeHtml(v.name)}</h3><p class="price"><strong>$${v.dailyRateCents/100}</strong> / day</p><button data-id="${v.id}" data-name="${escapeHtml(v.name)}">Choose vehicle</button></article>`).join('');
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Zanzibar car rental | TOETI</title><meta name="description" content="Request a no-deposit rental car in Zanzibar from local supplier Maluda Car Rental."><style>
*{box-sizing:border-box}body{margin:0;background:#f5f7fc;color:#0b1220;font-family:Inter,system-ui,sans-serif}header{background:#07152c;color:#fff;padding:20px max(5vw,24px);display:flex;justify-content:space-between;align-items:center}.brand{font-weight:950;letter-spacing:.14em}.brand i{font-style:normal;color:#8d71ff}.nav{font-size:14px;color:#c8d1e2}.hero{padding:72px max(5vw,24px) 48px;background:linear-gradient(135deg,#07152c 0%,#12244c 60%,#5733a6 150%);color:#fff}.hero small{color:#71e6ca;font-weight:800;text-transform:uppercase;letter-spacing:.15em}.hero h1{font-size:clamp(42px,7vw,78px);line-height:.98;margin:18px 0;max-width:850px;letter-spacing:-.05em}.hero p{font-size:18px;line-height:1.6;color:#d5dced;max-width:700px}.facts{display:flex;flex-wrap:wrap;gap:10px;margin-top:28px}.facts span{background:#ffffff12;border:1px solid #ffffff26;padding:10px 14px;border-radius:999px;font-size:14px}.wrap{padding:48px max(5vw,24px);max-width:1400px;margin:auto}.topline{display:flex;justify-content:space-between;align-items:end;gap:20px}.topline h2{font-size:32px;margin:0}.topline p{margin:0;color:#687387}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:18px;margin-top:28px}.car{background:#fff;border:1px solid #e4e8f1;border-radius:22px;padding:20px;box-shadow:0 10px 30px #1c31500a}.badge{display:inline-block;color:#3152c9;background:#edf1ff;border-radius:999px;padding:7px 10px;font-size:12px;font-weight:800}.carart{height:120px;margin:18px 0;border-radius:16px;background:linear-gradient(145deg,#e9edfa,#f7f8fd);display:grid;place-items:center;font-size:38px;font-weight:950;color:#6b75a1}.car h3{min-height:48px}.price strong{font-size:28px}.car button,.submit{width:100%;border:0;border-radius:12px;background:#6246d8;color:#fff;padding:13px;font-weight:850;cursor:pointer}.policy{margin-top:40px;background:#fff;border-radius:22px;padding:25px;border:1px solid #e4e8f1}.policy-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}.policy b{display:block;margin-bottom:5px}.pending{color:#93610b;background:#fff4d7;padding:12px;border-radius:12px}.modal{display:none;position:fixed;inset:0;z-index:5;background:#061020b8;padding:20px;overflow:auto}.modal.open{display:grid;place-items:center}.panel{width:min(620px,100%);background:#fff;border-radius:24px;padding:28px}.panel-head{display:flex;justify-content:space-between}.close{border:0;background:none;font-size:24px}.fields{display:grid;grid-template-columns:1fr 1fr;gap:14px}.fields label{font-size:13px;font-weight:750}.fields input,.fields select{display:block;width:100%;margin-top:6px;padding:12px;border:1px solid #ccd3e0;border-radius:10px;font:inherit}.wide{grid-column:1/-1}.notice{font-size:13px;color:#697386}.result{display:none;padding:18px;background:#edfaf6;border-radius:14px}.error{color:#a21d35}.footer{padding:35px max(5vw,24px);background:#07152c;color:#aebad1}@media(max-width:950px){.grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:580px){.grid,.policy-grid,.fields{grid-template-columns:1fr}.wide{grid-column:auto}.topline{display:block}.grid{gap:12px}.hero{padding-top:52px}}
</style></head><body><header><div class="brand">TOETI <i>GLOBAL</i></div><div class="nav">Rental Cars & Experiences</div></header><section class="hero"><small>Zanzibar · Tanzania</small><h1>Local cars. Clear prices. Zanzibar ready.</h1><p>Choose a vehicle and send a booking request. Maluda Car Rental confirms availability before any payment is requested.</p><div class="facts"><span>No deposit</span><span>Free airport, seaport & Stone Town delivery</span><span>Free cancellation up to 48 hours</span></div></section><main class="wrap"><div class="topline"><h2>Available rental options</h2><p>7 vehicles · Prices in USD</p></div><section class="grid">${cards}</section><section class="policy"><h2>What is confirmed</h2><div class="policy-grid"><p><b>Pickup and drop-off</b>Free within Stone Town, airport and seaport. Outside Stone Town: $10 one-way.</p><p><b>Deposit</b>No deposit required.</p><p><b>Cancellation</b>Free until 48 hours before pickup.</p></div><p class="pending"><b>Still awaiting supplier confirmation:</b> insurance coverage and excess/deductible.</p></section></main><div class="modal" id="modal"><form class="panel" id="booking"><div class="panel-head"><div><small>BOOKING REQUEST</small><h2 id="chosen">Vehicle</h2></div><button class="close" type="button" aria-label="Close">×</button></div><input type="hidden" name="vehicleId"><div class="fields"><label>Pickup date<input type="date" name="pickupDate" required></label><label>Return date<input type="date" name="returnDate" required></label><label class="wide">Pickup location<select name="pickupLocation" required><option>Stone Town</option><option>Zanzibar Airport</option><option>Zanzibar Seaport</option><option>Outside Stone Town</option></select></label><label>Full name<input name="customerName" required maxlength="100"></label><label>Email<input type="email" name="customerEmail" required maxlength="160"></label><label class="wide">Phone<input type="tel" name="customerPhone" required maxlength="40"></label></div><p class="notice">This is a request, not an instant confirmation. No payment will be collected now.</p><button class="submit">Send booking request</button><p class="error" id="error"></p><div class="result" id="result"></div></form></div><footer class="footer">TOETI Rental · A division of Sarose Select</footer><script>
const modal=document.querySelector('#modal'),form=document.querySelector('#booking');document.querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>{form.reset();form.vehicleId.value=b.dataset.id;document.querySelector('#chosen').textContent=b.dataset.name;document.querySelector('#result').style.display='none';modal.classList.add('open')});document.querySelector('.close').onclick=()=>modal.classList.remove('open');form.onsubmit=async(e)=>{e.preventDefault();const err=document.querySelector('#error');err.textContent='';const btn=form.querySelector('.submit');btn.disabled=true;btn.textContent='Sending…';try{const body=Object.fromEntries(new FormData(form));const r=await fetch('/api/bookings',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const data=await r.json();if(!r.ok)throw Error(data.error||'Could not create booking.');document.querySelector('#result').innerHTML='<strong>Request received</strong><p>Reference: '+data.reference+'</p><p>Total for '+data.rentalDays+' days: $'+(data.totalCents/100).toFixed(2)+'</p><p>Status: '+data.status+'</p>';document.querySelector('#result').style.display='block';btn.style.display='none'}catch(x){err.textContent=x.message}finally{btn.disabled=false;btn.textContent='Send booking request'}};
</script></body></html>`;
}

async function createBooking(request, env) {
  if (!env.DB) return json({ error: 'Booking storage is not configured.' }, 503);
  let input; try { input = await request.json(); } catch { return json({ error: 'Invalid JSON.' }, 400); }
  const required = ['vehicleId','pickupDate','returnDate','pickupLocation','customerName','customerEmail','customerPhone'];
  if (required.some(k => !String(input[k] ?? '').trim())) return json({ error: 'Complete all required fields.' }, 400);
  if (!/^\S+@\S+\.\S+$/.test(input.customerEmail) || String(input.customerName).length > 100 || String(input.customerEmail).length > 160 || String(input.customerPhone).length > 40) return json({ error: 'Invalid contact information.' }, 400);
  const vehicle = vehicles.find(v => v.id === input.vehicleId); if (!vehicle) return json({ error: 'Unknown vehicle.' }, 400);
  let totalCents, days; try { totalCents = quoteCents(vehicle.dailyRateCents, input.pickupDate, input.returnDate); days = totalCents / vehicle.dailyRateCents; } catch (e) { return json({ error: e.message }, 400); }
  const id = crypto.randomUUID(), reference = publicReference(), token = secureToken(), now = new Date().toISOString();
  try {
    await env.DB.batch([
      env.DB.prepare(`INSERT INTO bookings (id, reference, secure_token, supplier_id, vehicle_id, customer_name, customer_email, customer_phone, pickup_date, return_date, pickup_location, currency, daily_rate_cents, total_cents, status, created_at, updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(id, reference, token, supplier.id, vehicle.id, input.customerName.trim(), input.customerEmail.trim().toLowerCase(), input.customerPhone.trim(), input.pickupDate, input.returnDate, input.pickupLocation, vehicle.currency, vehicle.dailyRateCents, totalCents, 'REQUESTED', now, now),
      env.DB.prepare(`INSERT INTO booking_events (id, booking_id, event_type, from_status, to_status, payload_json, created_at) VALUES (?,?,?,?,?,?,?)`).bind(crypto.randomUUID(), id, 'BOOKING_REQUESTED', null, 'REQUESTED', JSON.stringify({ source: 'PUBLIC_ZANZIBAR_FORM' }), now),
      env.DB.prepare(`INSERT INTO audit_events (id, entity_type, entity_id, action, source, metadata_json, created_at) VALUES (?,?,?,?,?,?,?)`).bind(crypto.randomUUID(), 'booking', id, 'BOOKING_CREATED', 'PUBLIC_WEB', JSON.stringify({ reference, vehicleId: vehicle.id }), now)
    ]);
  } catch (e) { console.error('booking_persistence_failed', { message: e?.message }); return json({ error: 'Booking could not be saved. Please try again.' }, 503); }
  return json({ reference, status: 'REQUESTED', rentalDays: days, totalCents, currency: vehicle.currency }, 201);
}

export default { async fetch(request, env) {
  const url = new URL(request.url);
  if (request.method === 'GET' && (url.pathname === '/' || url.pathname === '/zanzibar/car-rental')) return new Response(page(), { headers: { 'content-type': 'text/html; charset=utf-8', 'content-security-policy': "default-src 'self'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; img-src 'self' data:; frame-ancestors 'none'; base-uri 'none'; form-action 'self'", 'x-content-type-options': 'nosniff', 'referrer-policy': 'strict-origin-when-cross-origin' } });
  if (request.method === 'GET' && url.pathname === '/api/inventory') return json({ destination: supplier.destination, supplier: { name: supplier.name, bookingMode: supplier.bookingMode }, vehicles, policies });
  if (request.method === 'POST' && url.pathname === '/api/bookings') return createBooking(request, env);
  if (request.method === 'GET' && url.pathname === '/api/health') return json({ status: 'ok', payments: new PaymentRouter().select({}) });
  return json({ error: 'Not found.' }, 404);
}};
